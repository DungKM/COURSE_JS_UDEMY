'use strict';

let hasDriversLicense = false;
const passTest = true;
if(passTest) hasDriverLicense = true; // use strict báo lỗi nhà phát triển
if(hasDriversLicense) console.log("Hello KeyMagic can drive !!");


const private = "Make in"; // private sẽ xuất hiện bằng câu lệnh -> use trict sẽ báo lỗi variable

